services:
   app:
      image: <DOCKER_USER>/docker-build-remote:latest
      environment:
         - SYNC_USER1=USERNAME:HASHED_PASSWORD
      volumes:
         - app_data:/data

volumes:
  app_data:
    driver: local